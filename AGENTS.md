# AGENTS.md

All agent guidance for this repository lives in `CLAUDE.md` at the repo root. Read it in full before doing any work here: it covers the branch workflow (two levels, `main` is live production), commands, environment, architecture, and the live-event safety rules.

It is the single source of truth for every coding agent. Do not duplicate its content here; earlier copies of this file drifted out of date and described a retired branch workflow.

Also read `TRACKER.md` (living checklist of bugs and planned work) before touching scoring, realtime, offline, or RLS-sensitive code.
